import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  title: {
    'margin-right': 'auto',
  },
  div: {
    'margin-right': '24px',
  },
}));

export default useStyles;
