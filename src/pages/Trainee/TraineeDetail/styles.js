import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    display: 'flex',
  },
  cover: {
    width: '150px',
  },
  button: {
    margin: '16px',
  },
});

export default useStyles;
