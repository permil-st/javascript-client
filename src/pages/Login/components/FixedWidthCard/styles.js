import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  cardContent: {
    padding: '24px',
  },
  card: {
    width: '400px',
  },
}));

export default useStyles;
